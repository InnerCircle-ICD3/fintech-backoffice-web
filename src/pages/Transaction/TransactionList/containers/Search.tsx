import SearchContainer from '@/components/layout/container/SearchContainer';

const Search = () => {
  return <SearchContainer onSearch={() => {}} onReset={() => {}}></SearchContainer>;
};

export default Search;
