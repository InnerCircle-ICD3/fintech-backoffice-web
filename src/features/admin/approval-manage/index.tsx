import AdminSection from '@/components/layout/section/admin';
import Text from '@/components/ui/text';

const ApprovalManage = () => {
  return (
    <AdminSection label={'승인요청 관리'}>
      <div>
        <Text>승인 관리</Text>
      </div>
    </AdminSection>
  );
};

export default ApprovalManage;
